$(document).ready(function() {
  $("#plant1").hover(
    function(event) {
      $('#central').css({
        "background-image": "url('<%= image_url "1-plant-a-tree.png" %>')"
    });
    },
    function(event) {
      $('#central').css({"background-image": "url('<%= image_url "pp-fleuve-plant.png" %>')"});
      $('#plant1').css({
        "color": "$green"
    });
    }
  );
});

$(document).ready(function() {
  $("#offer1").hover(
    function(event) {
      $('#central').css({
        "background-image": "url('<%= image_url "2-offer-it.png" %>')",
        "background-size": "cover"
      });
    },
    function(event) {
      $('#central').css({
        "background-image": "url('<%= image_url "pp-fleuve-plant.png" %>')",
        "background-size": "cover"
      });
    }
  );
});

$(document).ready(function() {
  $("#grow1").hover(
    function(event) {
      $('#central').css({
        "background": "url('<%= image_url "4-grow-my-forest.png" %>')",
        "background-size": "cover"

    });
    },
    function(event) {
      $('#central').css({
        "background-image": "url('<%= image_url "pp-fleuve-plant.png" %>')",
        "background-size": "cover"
      });
    }
  );
});

$(document).ready(function() {
  $("#share1").hover(
    function(event) {
      $('#central').css({
        "background": "url('<%= image_url "3-start-forest.png" %>')",
        "background-size": "cover"
    });
    },
    function(event) {
      $('#central').css({
        "background-image": "url('<%= image_url "pp-fleuve-plant.png" %>')"

    });
    }
  );
});
